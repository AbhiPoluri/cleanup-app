<Window x:Class="Cleanup.WhiteboardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Whiteboard" Width="1040" Height="700" MinWidth="820" MinHeight="560"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="CanResize"
        WindowStartupLocation="CenterScreen">
  <Window.Resources>
    <Style x:Key="TitleAction" TargetType="Button">
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="Padding" Value="9,4"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="Chrome" Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="5" Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Chrome" Property="Opacity" Value="0.78"/>
              </Trigger>
              <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="Chrome" Property="Opacity" Value="0.55"/>
              </Trigger>
              <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="Chrome" Property="Opacity" Value="0.4"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </Window.Resources>
  <Border x:Name="Root" CornerRadius="12" BorderThickness="1" MouseLeftButtonDown="Root_MouseDown">
    <DockPanel>
      <Grid DockPanel.Dock="Top" Margin="16,12,14,10">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/>
          <ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/>
          <ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/>
          <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <TextBlock x:Name="TitleLabel" Text="Whiteboard" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center"/>
        <Button x:Name="ProjectChip" Grid.Column="1" Style="{StaticResource TitleAction}"
                Padding="8,3" Margin="10,0,0,0" Click="Project_Click" ToolTip="Switch Whiteboard project">
          <TextBlock x:Name="ProjectLabel" FontSize="11.5" FontFamily="Consolas"/>
        </Button>
        <TextBlock x:Name="StatusLabel" Grid.Column="2" Margin="10,0" FontSize="11" FontFamily="Consolas" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
        <TextBlock x:Name="EngineLabel" Grid.Column="3" Margin="0,0,10,0" FontSize="11" FontFamily="Consolas" VerticalAlignment="Center"/>
        <Button x:Name="RemoteBtn" Grid.Column="4" Style="{StaticResource TitleAction}"
                Margin="0,0,7,0" Click="Remote_Click" ToolTip="Open the QR code for the iPhone remote">
          <TextBlock x:Name="RemoteLabel" Text="Phone remote" FontSize="11"/>
        </Button>
        <Button x:Name="MuteBtn" Grid.Column="5" Style="{StaticResource TitleAction}"
                Margin="0,0,7,0" Click="Mute_Click" ToolTip="Turn spoken replies on or off">
          <TextBlock x:Name="MuteLabel" FontSize="11"/>
        </Button>
        <Button x:Name="CloseBtn" Grid.Column="6" Style="{StaticResource TitleAction}"
                Padding="8,4" Click="Close_Click" ToolTip="Close Whiteboard">
          <TextBlock x:Name="CloseLabel" Text="✕" FontSize="11"/>
        </Button>
      </Grid>

      <Grid Margin="14,0,14,14">
        <Grid.ColumnDefinitions><ColumnDefinition Width="1.15*"/><ColumnDefinition Width="10"/><ColumnDefinition Width="1*"/></Grid.ColumnDefinitions>

        <Border x:Name="CameraCard" Grid.Column="0" CornerRadius="10" BorderThickness="1" Padding="10">
          <DockPanel>
            <Grid DockPanel.Dock="Bottom" Margin="0,9,0,0">
              <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
              <ComboBox x:Name="CameraBox" Width="105" Height="28" SelectionChanged="Camera_Changed"/>
              <Button x:Name="ResetBtn" Grid.Column="1" Content="reset frame" Margin="7,0,0,0" Padding="9,3" Click="Reset_Click"/>
              <Button x:Name="MicBtn" Grid.Column="3" Content="mic" Margin="7,0,7,0" Padding="12,3" Click="Mic_Click"/>
              <Button x:Name="LookBtn" Grid.Column="4" Content="Look now" Padding="12,3" Click="Look_Click"/>
            </Grid>
            <Border x:Name="CameraStage" CornerRadius="8" ClipToBounds="True">
              <Viewbox Stretch="Uniform">
                <Grid Width="640" Height="480">
                  <Image x:Name="CameraImage" Width="640" Height="480" Stretch="Fill"/>
                  <Canvas x:Name="CornerCanvas" Width="640" Height="480" Background="Transparent">
                    <Polygon x:Name="FramePolygon" Stroke="White" StrokeThickness="2" Fill="#16000000"/>
                    <Ellipse x:Name="P0" Tag="0" Width="20" Height="20" Fill="White" Stroke="#88000000" StrokeThickness="2" Cursor="SizeAll" MouseLeftButtonDown="Point_Down" MouseMove="Point_Move" MouseLeftButtonUp="Point_Up"/>
                    <Ellipse x:Name="P1" Tag="1" Width="20" Height="20" Fill="White" Stroke="#88000000" StrokeThickness="2" Cursor="SizeAll" MouseLeftButtonDown="Point_Down" MouseMove="Point_Move" MouseLeftButtonUp="Point_Up"/>
                    <Ellipse x:Name="P2" Tag="2" Width="20" Height="20" Fill="White" Stroke="#88000000" StrokeThickness="2" Cursor="SizeAll" MouseLeftButtonDown="Point_Down" MouseMove="Point_Move" MouseLeftButtonUp="Point_Up"/>
                    <Ellipse x:Name="P3" Tag="3" Width="20" Height="20" Fill="White" Stroke="#88000000" StrokeThickness="2" Cursor="SizeAll" MouseLeftButtonDown="Point_Down" MouseMove="Point_Move" MouseLeftButtonUp="Point_Up"/>
                  </Canvas>
                </Grid>
              </Viewbox>
            </Border>
          </DockPanel>
        </Border>

        <Border x:Name="ChatCard" Grid.Column="2" CornerRadius="10" BorderThickness="1" Padding="8">
          <DockPanel>
            <Border x:Name="InputBar" DockPanel.Dock="Bottom" CornerRadius="9" BorderThickness="1" Padding="8" Margin="0,7,0,0">
              <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <TextBox x:Name="InputBox" BorderThickness="0" Background="Transparent" FontSize="13" TextWrapping="Wrap" AcceptsReturn="True" MaxHeight="90" PreviewKeyDown="Input_KeyDown"/>
                <Button x:Name="SendBtn" Grid.Column="1" Content="↑" Width="34" Height="34" Margin="7,0,0,0" Click="Send_Click"/>
              </Grid>
            </Border>
            <ScrollViewer x:Name="Transcript" VerticalScrollBarVisibility="Auto"><StackPanel x:Name="Feed" Margin="2,2,2,8"/></ScrollViewer>
          </DockPanel>
        </Border>
      </Grid>
    </DockPanel>
  </Border>
</Window>
